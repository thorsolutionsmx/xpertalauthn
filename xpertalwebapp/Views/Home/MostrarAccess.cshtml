@model Dictionary<string, string>

Los datos del token de acceso


<p>
    <table class="table">
        <thead>
            <tr>
                <td>Claim</td>
                <td>Valor</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var _item in (Dictionary<string, string>)Model)
            {
                <tr>
                    <td>@_item.Key</td>
                    <td>@_item.Value</td>
                </tr>

            }
            <a href="c:\discod\code\curso\webmodernapp\webmodernapp\views\acceso\muestratokenaccess.cshtml">c:\discod\code\curso\webmodernapp\webmodernapp\views\acceso\muestratokenaccess.cshtml</a>
        </tbody>
    </table>

</p>